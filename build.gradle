plugins {
    id 'com.ewerk.gradle.plugins.jaxb2' version '1.0.0'
}

dependencies {
    compile project(':ringingmaster-persist')
    compile project(':ringingmaster-engine')
}

jaxb2 {
    xjc {
        'cc-methods-classes' {
            basePackage = 'com.cccbr.generated.method'
            schema = 'resources/methods.xsd'
        }
    }
}